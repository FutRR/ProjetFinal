{% extends 'base.html.twig' %}

{% block title %}{{ niveau }}{% endblock %}

{% block body %}

    <a href="{{ path('app_niveau')}}">Liste des niveaux</a>
    <h1>{{niveau}}</h1>

    {% if is_granted('ROLE_ADMIN') %}
        <a class="btn add-btn" href="{{ path('new_etape') }}">Ajouter une étape</a>
        <a class="btn add-btn" href="{{ path('edit_niveau', {'id': niveau.id})}}">Modifier le niveau</a>
    {% endif %}

    <a class="btn add-btn" href="">Commencer le cours</a>


    {% for etape in etapes %}
        <h2>{{ etape.ordre }} - <a href="{{ path('show_etape', {'id': etape.id})}}">{{ etape }}</a> </h2>
        <p> {{ etape.description }} </p>
    {% endfor %}

{% endblock %}